import React from 'react'

const AddPlayersToLeague = ({handleBack}) => {
  return (
    <div>
      <h1>Add Players</h1>
      <button onClick={handleBack}>BACK</button>
      <button>Fucking Send It</button>
    </div>
  )
}

export default AddPlayersToLeague